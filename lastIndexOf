/**
 * Write a method named lastIndexOf that accepts an array of integers and an integer value as its parameters and returns the last index at which the value occurs in the array. 
 * The method should return -1 if the value is not found. For example, in the list containing {74, 85, 102, 99, 101, 85, 56}, the last index of the value 85 is 5.
 */
 
 
 public static int lastIndexOf(int[] arr, int value) {
    int length = arr.length-1;
    for (int i = length; i >= 0; i--) {
        if (arr[i] == value) {
            return i;
        }
    }
    return -1;
}
